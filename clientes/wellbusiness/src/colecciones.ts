/**
 * Contenido inicial de Wellbusiness — un arreglo por colección, cada ítem
 * envuelto con `item(id, orden, campos)` (ver `./ayudantes.ts`, Fase 3).
 *
 * Placeholder por ahora (Fase 2): arreglos vacíos, uno por cada colección
 * declarada en `./esquemas.ts`, para que `index.ts` compile. La Fase 3
 * transcribe aquí, literalmente, el contenido real de
 * `apps/web/src/data/*.ts` — cero contenido inventado.
 */
export const categorias: unknown[] = [];
export const productos: unknown[] = [];
export const servicios: unknown[] = [];
export const sectores: unknown[] = [];
export const zonas: unknown[] = [];
export const preguntas: unknown[] = [];
export const marcas: unknown[] = [];
export const accesosRapidos: unknown[] = [];
export const valores: unknown[] = [];
